name: Unit tests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:

    runs-on: ${{ matrix.os }}
    strategy:
      max-parallel: 20
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        fetch-tags: true

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Setup test environment
      run: pip install .[test]

    - name: Run linters
      run: brundle

    - name: Run unit tests
      run: pytest

    # - name: Run build
    #   run: python boil.py build

    # - name: Run clean
    #   run: python boil.py clean

    # - name: Run upgrade
    #   run: python boil.py upgrade
